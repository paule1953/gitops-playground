apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: petclinic-plain
  namespace: fluxv1-staging
spec:
  rules:
    - host: "fluxv1-petclinic-plain-staging"
      http:
        paths:
          - path: /
            backend:
              serviceName: spring-petclinic-plain
              servicePort: http
